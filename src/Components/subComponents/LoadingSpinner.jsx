import React from "react";
import { Spinner } from "react-bootstrap";

function LoadingSpinner() {
  return <Spinner animation="border" variant="dark" size="lg"></Spinner>;
}

export default LoadingSpinner;
